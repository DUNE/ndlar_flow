# Generates the mid-level event built data for charge data (i.e. hits and
# external triggers)

flow:
  source: raw_events
  #source: calib_prompt_hits
  #stages: [temp_hit_builder, calib_hit_merger]
  stages: [calib_hit_merger]
  drop: []


resources:
  - !include yamls/fsd_flow/resources/RunDataMC.yaml
  - !include yamls/fsd_flow/resources/LArData.yaml
  - !include yamls/fsd_flow/resources/GeometryData.yaml

raw_events:
  classname: H5FlowDatasetLoopGenerator
  path: h5flow.modules
  dset_name: 'charge/raw_events'
  params:
    chunk_size: 32

calib_hit_merger:
  !include yamls/fsd_flow/reco/charge/CalibHitMerger.yaml

